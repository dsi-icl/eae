{
    "properties": {
        "startDate": { "type": "string", "format": "date" },
        "endDate": { "type": "string", "format": "date" },
        "algorithm": {"type": "string"},
        "aggregationLevel": {"type": "array"},
        "aggregationValues": {"type": "array"},
        "sample": { "type": "number", "maximum": 1 }
    }
}
